# FreshMind - AI 쇼핑 어시스턴트 🛒🤖

마켓컬리 스타일의 AI 챗봇 기반 식재료 쇼핑 플랫폼

## 📋 프로젝트 개요

개인화된 AI 추천과 구매이력 분석을 통한 스마트 쇼핑 경험 제공

## 🎯 핵심 기능

### 1. 개인화 프로필 시스템
- 성별, 연령대, 직업, 혼인여부 기반 맞춤 추천
- 가상 유저 계정 3개 (개발용)
- 프로필 전환으로 다양한 추천 경험

### 2. 스마트 상품 정렬
- **개인화 추천순**: 구매이력 + 프로필 기반 추천 (구매이력 80% 가중치)
- **인기순, 신상품순, 가격순, 별점순**
- **카테고리 필터** (10개 카테고리)
- **핫한 요리 탭**: 트렌드 상품 추천 (밀키트/간편식 우선)

### 3. 구매이력 분석
- **구매 요약 배너**: 최근 한 달 구매 패턴 분석
- 반복 구매 상품 및 선호 카테고리 표시
- 개인화된 메시지 템플릿

### 4. AI 챗봇 (듀얼 모델)
- **GPT-4o-mini** 또는 **Gemini** 선택 가능
- 감정 분석 기반 상품 추천 (3-5개)
- 구매이력 기반 맞춤 추천 이유 설명
- 상품 추천 로직:
  - ① 반복 구매 상품: 100 + (구매횟수 × 10점) + 최근 구매 보너스
  - ② 최근 구매 카테고리: 60~90점
  - ③ 선호 카테고리 미구매 상품: 50~75점
  - ④ 구매이력 가중치: **80%** (강화)
  - ⑤ 프로필 & 인기도: 보조 가중치

## 🏗️ 기술 스택

| 계층 | 기술 | 설명 |
|------|------|------|
| **Frontend** | Next.js 15 + TypeScript | React 웹 앱 |
| **Backend** | FastAPI (Python) | REST API |
| **Database** | PostgreSQL | 관계형 DB |
| **AI** | OpenAI GPT-4o-mini + Google Gemini | 듀얼 AI 모델 |
| **Styling** | Tailwind CSS | 반응형 UI |

## 🚀 시작하기

### Prerequisites
- Node.js 18+
- Python 3.10+
- PostgreSQL 15+

### 환경 설정

#### 1. API 키 설정
프로젝트 루트에 `secret.json` 파일 생성:

```json
{
  "openai_api_key": "your-openai-api-key",
  "googleai_api_key": "your-google-ai-api-key"
}
```

#### 2. Database 설정
```bash
# PostgreSQL 데이터베이스 생성
createdb freshmind_db

# 스키마 생성
psql freshmind_db -f backend/database/schema.sql
```

### 설치 및 실행

#### Backend
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
```

또는 실행 스크립트 사용:
```bash
./start-backend.sh
```

#### Frontend
```bash
cd frontend
npm install
npm run dev
```

또는 실행 스크립트 사용:
```bash
./start-frontend.sh
```

- Frontend: http://localhost:3000
- Backend API: http://localhost:8001
- API Docs: http://localhost:8001/docs

## 📦 프로젝트 구조

```
freshmind-project/
├── frontend/                   # Next.js 웹 앱
│   ├── app/
│   │   ├── components/        # React 컴포넌트
│   │   │   ├── ChatBotModal.tsx       # AI 챗봇 (듀얼 모델)
│   │   │   ├── PurchaseSummaryBanner.tsx  # 구매 요약
│   │   │   └── ...
│   │   ├── data/              # Mock 데이터
│   │   │   ├── products.ts           # 175개 상품
│   │   │   ├── mockUsers.ts          # 3개 유저 계정
│   │   │   └── mockPurchaseHistory.ts  # 구매이력 (150건)
│   │   ├── contexts/          # React Context
│   │   ├── types/             # TypeScript 타입
│   │   └── utils/             # 유틸리티 함수
│   └── ...
├── backend/                    # FastAPI 백엔드
│   ├── app/
│   │   ├── main.py            # API 엔드포인트
│   │   ├── models.py          # DB 모델
│   │   ├── database.py        # DB 연결
│   │   ├── routers/           # API 라우터
│   │   │   ├── users.py       # 사용자 API
│   │   │   └── chatbot.py     # 챗봇 API (듀얼 모델)
│   │   └── services/          # 비즈니스 로직
│   │       ├── chatbot.py     # AI 챗봇 서비스
│   │       └── purchase_insights.py  # 구매 인사이트
│   └── database/
│       └── schema.sql         # DB 스키마
├── secret.json                # API 키 (gitignore)
├── start-backend.sh           # 백엔드 실행 스크립트
├── start-frontend.sh          # 프론트엔드 실행 스크립트
└── README.md
```

## 📊 데이터베이스 구조

### 주요 테이블

**Users** - 사용자 프로필
```sql
user_id, name, birth_date, gender, age_group,
occupation, marital_status
```

**Products** - 상품 정보 (175개)
```sql
product_id, name, category, price, rating,
target_age_groups, target_gender, used_in, tags
```

**Purchase_History** - 구매이력 (150건)
```sql
purchase_id, user_id, product_id, quantity, purchased_at
```

**Chat_Messages** - 챗봇 대화
```sql
message_id, user_id, sender, message_text,
sentiment, sentiment_score, recommended_products
```

## 🤖 AI 챗봇 특징

### 듀얼 모델 시스템
- **GPT-4o-mini**: OpenAI의 빠르고 정확한 모델
- **Gemini 1.5 Flash**: Google의 최신 AI 모델
- 채팅창 하단에서 모델 실시간 전환 가능

### 추천 알고리즘
```
1. 구매이력 분석 (최우선)
   - 반복 구매 상품 파악
   - 최근 구매 카테고리 분석
   - 선호 카테고리 TOP 3 추출
   ↓
2. 통합 점수 계산 (모든 상품)
   - 구매이력 점수 (80%) ⬆️ 강화
   - 프로필 점수 (보조, 구매이력 있으면 낮게)
   - 인기도 점수 (보조, 구매이력 있으면 낮게)
   ↓
3. 상위 30개 상품 선정
   ↓
4. AI 모델에게 전달
   - 사용자 메시지 분석
   - 감정 & 키워드 추출
   - 최적 3~5개 선택
   ↓
5. 최종 추천 + 이유 설명
```

#### 구매이력 점수 계산
- **반복 구매 상품**: 100 + (구매횟수 × 10점) + 최근 구매 보너스
- **최근 구매 카테고리**: 60점 + (1개월 내 +30점)
- **최근 카테고리의 미구매 상품**: 50점 + (1개월 +25, 2개월 +15)
- **선호 카테고리 미구매 상품**: 30점

## 📱 주요 화면

1. **메인 페이지**: 상품 목록, 정렬/필터, 검색
2. **구매 요약 배너**: 개인화된 구매 패턴 메시지
3. **AI 챗봇**: 듀얼 모델 선택, 상품 추천
4. **프로필 전환**: 3개 가상 유저 계정 전환

## 🎨 UI/UX 특징

- **반응형 디자인**: 모바일/태블릿/데스크톱 최적화
- **깔끔한 회색 톤**: 모던하고 전문적인 디자인
- **직관적인 인터페이스**: 쉬운 네비게이션과 명확한 정보 전달
- **실시간 피드백**: 로딩 상태, 에러 핸들링

## 🔌 API 엔드포인트

### 사용자
- `GET /api/users/{user_id}` - 프로필 조회
- `GET /api/users/{user_id}/purchase-summary` - 구매 요약

### 상품
- `GET /api/products` - 상품 목록
- `GET /api/products/recommended` - 추천 상품

### 챗봇
- `POST /api/chatbot/chat` - 챗봇 메시지 전송
  ```json
  {
    "message": "아이 간식 추천해줄래?",
    "user_profile": {...},
    "products": [...],
    "purchase_history": [...],
    "model": "gpt" // or "gemini"
  }
  ```

## 🛠️ 개발 모드

PostgreSQL 없이도 개발 가능 (Mock 데이터 사용):
- `frontend/app/data/products.ts` - 175개 상품
- `frontend/app/data/mockUsers.ts` - 3개 유저
- `frontend/app/data/mockPurchaseHistory.ts` - 구매이력 (150건)

## 📝 라이선스

MIT

## 👥 개발자

정윤님

---

**Version**: 1.0.0 (Production Ready)  
**Last Updated**: 2026-01-09
