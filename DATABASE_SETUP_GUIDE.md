# 데이터베이스 설정 및 더미데이터 생성 가이드

**작성일**: 2026-01-05  
**대상**: 데이터베이스 설정 및 더미데이터 생성을 담당하는 동료  
**목적**: PostgreSQL 데이터베이스 설정 및 구매이력 기반 추천 시스템을 위한 더미데이터 생성

---

## 📋 목차

1. [프로젝트 배경](#프로젝트-배경)
2. [데이터베이스 설정](#데이터베이스-설정)
3. [더미데이터 생성 순서](#더미데이터-생성-순서)
4. [각 스크립트 상세 설명](#각-스크립트-상세-설명)
5. [검증 방법](#검증-방법)
6. [문제 해결](#문제-해결)

---

## 프로젝트 배경

### 프로젝트 목적
**FreshMind**는 유저의 **구매이력 + 프로필 정보**를 기반으로 개인화된 상품 추천을 제공하는 쇼핑 플랫폼입니다.

### 필요한 더미데이터
1. **가상 유저 계정 3개**: 구매 패턴이 다른 테스트 유저
2. **구매이력 데이터**: 최근 6개월간의 구매 이력 (유저당 50~200건)

### 유저 페르소나
- **유저 1 (김지은)**: 20대 여성, 자취생, 간편식 선호
- **유저 2 (박민수)**: 30대 남성, 기혼, 밀키트·요리 선호
- **유저 3 (이영희)**: 40대 여성, 기혼, 건강식·아동 식품 선호

---

## 데이터베이스 설정

### 1. PostgreSQL 설치 확인

```bash
# PostgreSQL 설치 확인
psql --version

# 설치되어 있지 않다면 (macOS)
brew install postgresql@15
brew services start postgresql@15
```

### 2. 데이터베이스 생성

```bash
# PostgreSQL 접속
psql postgres

# 데이터베이스 생성
CREATE DATABASE freshmind_db;

# 사용자 확인 (필요시 생성)
CREATE USER "jejeong-yun" WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE freshmind_db TO "jejeong-yun";

# 종료
\q
```

### 3. 연결 정보 확인

현재 스크립트에서 사용하는 연결 정보:
- **Database**: `freshmind_db`
- **User**: `jejeong-yun`
- **Host**: `localhost`
- **Port**: `5432`

> ⚠️ **주의**: 사용자 이름이 다르다면 스크립트의 연결 정보를 수정해야 합니다.

---

## 더미데이터 생성 순서

### ⚠️ 중요: 반드시 아래 순서대로 실행하세요!

```
1. schema.sql 실행          → 테이블 생성 + 유저 3명 생성
2. load_products_to_db.py   → 상품 100개 데이터 생성
3. load_purchase_history_to_db.py → 구매이력 더미데이터 생성
```

---

## 각 스크립트 상세 설명

### Step 1: schema.sql 실행

**파일 위치**: `backend/database/schema.sql`

**역할**:
- 모든 테이블 생성 (users, products, purchase_history, chat_messages)
- 가상 유저 계정 3개 자동 생성

**실행 방법**:
```bash
cd backend/database
psql -U jejeong-yun -d freshmind_db -f schema.sql
```

**또는**:
```bash
psql freshmind_db < backend/database/schema.sql
```

**예상 결과**:
```
CREATE TABLE
CREATE TABLE
CREATE TABLE
CREATE TABLE
INSERT 0 3  ← 유저 3명 생성됨
```

**검증**:
```bash
psql freshmind_db -c "SELECT user_id, name, age_group, gender FROM users;"
```

**예상 출력**:
```
 user_id |  name  | age_group | gender 
---------+--------+-----------+--------
       1 | 김지은 | 20s       | F
       2 | 박민수 | 30s       | M
       3 | 이영희 | 40s       | F
```

---

### Step 2: load_products_to_db.py 실행

**파일 위치**: `backend/database/load_products_to_db.py`

**역할**:
- `products.json` 파일에서 상품 데이터 읽기
- PostgreSQL의 `products` 테이블에 100개 상품 삽입

**사전 요구사항**:
- `backend/database/products.json` 파일이 존재해야 함
- Python 가상환경에 `psycopg2-binary` 패키지 설치 필요

**실행 방법**:
```bash
# 1. 가상환경 활성화
cd backend
source .venv/bin/activate

# 2. 필요한 패키지 설치 (처음 한 번만)
pip install psycopg2-binary

# 3. 스크립트 실행
cd database
python3 load_products_to_db.py
```

**예상 결과**:
```
============================================================
📦 FreshMind 상품 데이터 Import
============================================================
✅ PostgreSQL 연결 성공!
🗑️  기존 products 데이터 삭제 완료

📖 JSON 파일 로드 완료: 100개 상품

🚀 데이터 삽입 시작...

  ✓ 10/100 처리 중...
  ✓ 20/100 처리 중...
  ...

✅ 데이터 삽입 완료!

📊 삽입 결과
============================================================
  • 성공: 100개
  • 실패: 0개
  • DB 총 상품 수: 100개
```

**검증**:
```bash
psql freshmind_db -c "SELECT COUNT(*) FROM products;"
# 예상: 100
```

---

### Step 3: load_purchase_history_to_db.py 실행

**파일 위치**: `backend/database/load_purchase_history_to_db.py`

**역할**:
- 유저별 구매 패턴에 맞춰 최근 6개월 구매이력 생성
- 유저당 50~200건의 구매이력 생성
- 반복 구매 로직 포함

**사전 요구사항**:
- ✅ `schema.sql` 실행 완료 (users 테이블에 데이터 존재)
- ✅ `load_products_to_db.py` 실행 완료 (products 테이블에 데이터 존재)

**실행 방법**:
```bash
# 가상환경이 활성화되어 있다면
cd backend/database
python3 load_purchase_history_to_db.py
```

**예상 결과**:
```
============================================================
🛒 FreshMind 구매이력 더미데이터 생성
============================================================
✅ PostgreSQL 연결 성공!
🗑️  기존 purchase_history 데이터 삭제 완료

📖 유저 정보 로드 완료: 3명
   • [1] 김지은 (20s, F)
   • [2] 박민수 (30s, M)
   • [3] 이영희 (40s, F)
📦 상품 정보 로드 완료: 100개

🚀 구매이력 데이터 생성 시작...

  ✅ [1] 김지은: 6건 생성 완료
  ✅ [2] 박민수: 15건 생성 완료
  ✅ [3] 이영희: 18건 생성 완료

✅ 데이터 삽입 완료!

📊 삽입 결과
============================================================
  • 총 구매이력: 39건

👥 유저별 구매이력:
  • [1] 김지은: 6건
  • [2] 박민수: 15건
  • [3] 이영희: 18건

  • DB 총 구매이력 수: 39건
```

**검증**:
```bash
# 전체 구매이력 수 확인
psql freshmind_db -c "SELECT COUNT(*) FROM purchase_history;"

# 유저별 구매이력 확인
psql freshmind_db -c "
SELECT 
    u.name,
    COUNT(*) as purchase_count,
    MIN(ph.purchased_at) as first_purchase,
    MAX(ph.purchased_at) as last_purchase
FROM purchase_history ph
JOIN users u ON ph.user_id = u.user_id
GROUP BY u.name
ORDER BY u.user_id;
"
```

---

## 검증 방법

### 전체 데이터 검증 스크립트

```bash
# 모든 테이블 데이터 확인
psql freshmind_db << EOF
\echo '=== Users 테이블 ==='
SELECT user_id, name, age_group, gender FROM users;

\echo '\n=== Products 테이블 ==='
SELECT COUNT(*) as total_products FROM products;

\echo '\n=== Purchase History 테이블 ==='
SELECT 
    u.name,
    COUNT(*) as purchase_count
FROM purchase_history ph
JOIN users u ON ph.user_id = u.user_id
GROUP BY u.name;

\echo '\n=== 최근 구매이력 샘플 (최근 10건) ==='
SELECT 
    u.name,
    p.name as product_name,
    ph.quantity,
    ph.purchased_at
FROM purchase_history ph
JOIN users u ON ph.user_id = u.user_id
JOIN products p ON ph.product_id = p.product_id
ORDER BY ph.purchased_at DESC
LIMIT 10;
EOF
```

### 예상 결과

```
=== Users 테이블 ===
 user_id |  name  | age_group | gender 
---------+--------+-----------+--------
       1 | 김지은 | 20s       | F
       2 | 박민수 | 30s       | M
       3 | 이영희 | 40s       | F

=== Products 테이블 ===
 total_products 
----------------
            100

=== Purchase History 테이블 ===
  name  | purchase_count 
--------+-----------------
 김지은 |              6
 박민수 |             15
 이영희 |             18
```

---

## 문제 해결

### 문제 1: PostgreSQL 연결 실패

**에러 메시지**:
```
connection to server at "localhost" (::1), port 5432 failed: Connection refused
```

**해결 방법**:
```bash
# PostgreSQL 서비스 시작 (macOS)
brew services start postgresql@15

# 또는
pg_ctl -D /usr/local/var/postgres start
```

### 문제 2: 데이터베이스가 없음

**에러 메시지**:
```
FATAL: database "freshmind_db" does not exist
```

**해결 방법**:
```bash
psql postgres -c "CREATE DATABASE freshmind_db;"
```

### 문제 3: 사용자 권한 오류

**에러 메시지**:
```
FATAL: password authentication failed for user "jejeong-yun"
```

**해결 방법**:
1. 사용자 이름이 다른 경우: 스크립트의 `user` 파라미터 수정
2. 비밀번호가 필요한 경우: `~/.pgpass` 파일 생성 또는 환경 변수 설정

**스크립트 수정 위치**:
- `load_products_to_db.py` (20-24번 줄)
- `load_purchase_history_to_db.py` (20-24번 줄)

```python
conn = psycopg2.connect(
    dbname="freshmind_db",
    user="your_username",  # ← 여기 수정
    host="localhost",
    port="5432"
)
```

### 문제 4: products.json 파일이 없음

**에러 메시지**:
```
❌ JSON 파일 읽기 실패: [Errno 2] No such file or directory
```

**해결 방법**:
- `backend/database/products.json` 파일이 존재하는지 확인
- 없다면 프로젝트의 다른 위치에서 찾거나, 상품 데이터를 먼저 생성해야 함

### 문제 5: users 테이블에 데이터가 없음

**에러 메시지**:
```
❌ users 테이블에 데이터가 없습니다!
```

**해결 방법**:
- `schema.sql`을 먼저 실행했는지 확인
- `psql freshmind_db -c "SELECT * FROM users;"` 로 확인

### 문제 6: products 테이블에 데이터가 없음

**에러 메시지**:
```
❌ products 테이블에 데이터가 없습니다!
```

**해결 방법**:
- `load_products_to_db.py`를 먼저 실행했는지 확인
- `psql freshmind_db -c "SELECT COUNT(*) FROM products;"` 로 확인

---

## 빠른 실행 체크리스트

다음 순서대로 실행하면 됩니다:

- [ ] PostgreSQL 설치 및 실행 확인
- [ ] `freshmind_db` 데이터베이스 생성
- [ ] `schema.sql` 실행 → 테이블 + 유저 3명 생성
- [ ] `load_products_to_db.py` 실행 → 상품 100개 생성
- [ ] `load_purchase_history_to_db.py` 실행 → 구매이력 생성
- [ ] 검증 스크립트 실행 → 모든 데이터 확인

---

## 파일 구조 요약

```
backend/database/
├── schema.sql                          # 테이블 생성 + 유저 3명
├── products.json                       # 상품 데이터 (100개)
├── load_products_to_db.py             # 상품 데이터 삽입 스크립트
└── load_purchase_history_to_db.py     # 구매이력 더미데이터 생성 스크립트
```

---

## 추가 정보

### 유저별 구매 패턴 상세

**유저 1 (김지은, 20대 여성)**:
- 구매 빈도: 월 1회
- 선호 카테고리: 간편식/밀키트 (40%), 냉동식품 (25%), 즉석식품 (20%), 음료/차 (15%)
- 구매 수량: 1-2개
- 반복 구매 확률: 30%

**유저 2 (박민수, 30대 남성)**:
- 구매 빈도: 월 2-3회
- 선호 카테고리: 밀키트 (35%), 해산물 (20%), 육류/계란 (15%), 양념/오일 (15%), 채소 (15%)
- 구매 수량: 2-3개
- 반복 구매 확률: 25%

**유저 3 (이영희, 40대 여성)**:
- 구매 빈도: 월 2-4회
- 선호 카테고리: 밀키트 (30%), 냉동식품 (20%), 유제품 (15%), 과일 (15%), 채소 (20%)
- 구매 수량: 3-5개
- 반복 구매 확률: 35%

---

## 문의사항

문제가 발생하거나 질문이 있으시면:
1. 에러 메시지를 복사해서 공유해주세요
2. 실행한 명령어와 순서를 알려주세요
3. `psql freshmind_db -c "SELECT version();"` 결과를 공유해주세요

---

**작성자**: 개발팀  
**최종 수정일**: 2026-01-05

